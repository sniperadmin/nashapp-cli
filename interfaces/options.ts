export interface Options {
  skipPrompts: any,
  git: any,
  template: any,
  childTemplate: string,
  runInstall: any,
  targetDirectory: string,
  templateDirectory: string,
  name: string,
  email: string
}
